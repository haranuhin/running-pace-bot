services:
  - type: web
    name: running-pace-bot
    env: python
    plan: free
    buildCommand: pip install python-telegram-bot==20.7
    startCommand: python bot.py
    envVars:
      - key: BOT_TOKEN
        value: YOUR_BOT_TOKEN_HERE
